name: edusage_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  # Firebase Services (ALL COMPATIBLE WITH Dart 3.10+)
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.3.1

  
  
  # State Management
  provider: ^6.1.1

  # UI & Animations
  animate_do: ^3.3.4
  google_fonts: ^6.1.0

  # Icons
  lucide_icons: ^0.257.0
  cupertino_icons: ^1.0.8

  # Utilities
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true